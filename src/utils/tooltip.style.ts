import { css } from "styled-components"

export const tooltipStyle = css`
.tooltip{
    display: flex;
    flex-direction: column;
    padding: .5rem;
    gap: .5rem;
    font: 12px 'Inter';
    color: #002030;
}
`